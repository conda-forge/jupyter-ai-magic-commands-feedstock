context:
  version: "0.0.4"

package:
  name: jupyter-ai-magic-commands
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/j/jupyter-ai-magic-commands/jupyter_ai_magic_commands-${{ version }}.tar.gz
  sha256: 6a4ba9b98d04358367a0c1a3cb7a063a326d894b2a093069d861442ee2790834

build:
  noarch: python
  script: python -m pip install . -vv
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - hatchling >=1.4.0
  run:
    - python >=${{ python_min }}
    - click >=8.1.0,<9
    - ipython
    - jupyter-ai-litellm >=0.0.2
    - pydantic >=2.10.0,<3

tests:
  - python:
      imports:
        - jupyter_ai_magic_commands
      pip_check: true
    requirements:
      run:
        - python ${{ python_min }}.*

about:
  homepage: https://github.com/jupyter-ai-contrib/jupyter-ai-magic-commands
  summary: "Magic commands for using LLMs in Jupyter notebooks"
  license: BSD-3-Clause
  license_file: LICENSE
  documentation: https://jupyter-ai.readthedocs.io/
  repository: https://github.com/jupyter-ai-contrib/jupyter-ai-magic-commands

extra:
  recipe-maintainers:
    - jtpio
    - dlqqq
    - andrii-i
    - conda-forge/jupyter-ai
